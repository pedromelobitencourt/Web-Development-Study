let double = function(a) {
    return a * 2;
};

double = (a) => { return a * 2 };

double = a => { return a * 2 }; // If there is only one parameter

double = a => a * 2; // implicit return (only one line)

let hey = function() {
    return 'Hey';
}

hey = () => 'Hey';

hey = _ => 'Hey'; // '_' is a parameter
const NAME = 'Rebeca';
const concatenation = 'Hello ' + NAME + '!';
const template = `Hello, ${NAME}!`;

console.log(template);


// Expressions
console.log(`1 + 1 = ${1 + 1}`);

const up = text => text.toUpperCase(); // 'text': parameter

console.log(`Hey... ${up('attention')}!`);